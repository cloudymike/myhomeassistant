# Example swith using the blue LED on ESP board

This is a very simple example to use as a base for controlling 
something from homeassistant. It will allow you 
to control the blue LED on the board. The code includes
anything needed for self discovery

The setup assume that you are running the home assistant server on the 
local server.